# 2026-06-11 开发日志

## 今日完成

- 确定项目题目：基于 EfficientFormerV2 与增强训练策略的 EuroSAT 遥感土地利用图像分类系统设计与实现。
- 明确 GitHub 仓库使用 Public 可见性。
- 确定课程原始材料不上传到 GitHub。
- 建立项目初始化计划，包括 docs、dev_logs、配置和代码骨架。
- 完成第一阶段项目初始化并推送到 GitHub。
- 完成第二阶段最小代码骨架：配置读取、训练入口、评估入口、预测入口、模型与数据占位模块、Streamlit 外壳。

## 关键决策

- 使用 PyTorch、torchvision、timm 和 Streamlit 作为主要技术栈。
- 使用会话更新方式维护每日开发日志，不创建后台定时自动化。
- Public 仓库只同步代码、文档、配置和开发日志。
- 数据集、模型权重和实验输出仅保留在本地。
- 第二阶段脚本以 scaffold 形式存在，优先保证结构稳定，不提前实现完整训练逻辑。

## 问题与风险

- 当前 Python 环境尚未安装 torch、torchvision、timm 和 streamlit。
- 后续训练需要确认本机硬件性能，必要时先使用小批量或少轮数实验。
- Public 仓库需要持续避免上传课程原件、数据集和模型权重。

## 下一步待办

- 实现真实 EuroSAT 数据加载和数据划分。
- 实现 EfficientFormerV2 模型构建。
- 添加基础训练循环和单 batch smoke test。
- 补充评估指标和本地输出目录结构。

## 验证记录

- 已确认本地 Git 可用。
- 已确认 GitHub 远程仓库存在且当前为空。
- 已确认当前目录尚未初始化为 Git 仓库。
- 已验证课程原件和 `.DS_Store` 被 `.gitignore` 正确排除。
- 已推送首次提交到 GitHub。
- 已运行 `python3 -m compileall src app`，语法检查通过。
- 已运行训练、评估、预测 scaffold 入口，均可读取默认配置并正常退出。
- 已运行 `python3 app/streamlit_app.py`，在未安装 Streamlit 时能给出明确依赖提示。
