# 2026-06-12 开发日志

## 今日完成

- 继续推进第二阶段：最小 Python 代码骨架建立。
- 确认当前第二阶段改动包括配置读取、训练入口、评估入口、预测入口和 Streamlit 外壳。
- 准备将第二阶段骨架提交并同步到 GitHub。
- 开始推进第三阶段：实现 EuroSAT 数据加载模块和 `data_smoke` 检查入口。
- 安装项目依赖，包括 torch、torchvision、timm 和 streamlit。
- 下载 EuroSAT 数据集并完成真实 batch smoke test。
- 开始推进第四阶段：实现 EfficientFormerV2 模型构建、设备选择和训练 smoke test。
- 继续推进第四阶段：实现短程 baseline 训练循环、验证循环、metrics JSON 和 best checkpoint 保存。
- 继续完善第四阶段：实现独立评估脚本，支持加载 checkpoint 并输出整体与逐类指标。
- 运行中等规模 baseline 实验，获得比 quick baseline 更有参考价值的初步指标。
- 增加实验结果汇总工具，将训练与评估 JSON 汇总为 CSV 和 Markdown 表格。
- 开始推进第五阶段：新增 baseline/enhanced 配置，并在训练循环中加入 Mixup 支持。
- 运行同规模增强实验 `enhanced_20b`，并用独立评估脚本验证 checkpoint。

## 关键决策

- 第二阶段先收口为可运行 scaffold，不下载 EuroSAT，不安装深度学习依赖，不启动完整训练。
- 后续第三阶段再实现真实 EuroSAT 数据加载和 EfficientFormerV2 baseline。
- 数据模块先支持配置检查和依赖检查，真实下载与 batch 验证等依赖安装后执行。
- 当前 timm 版本可用模型名为 `efficientformerv2_s0`，因此将配置中的模型名调整为该版本可创建的名称。

## 问题与风险

- 当前环境缺少 torch、torchvision、timm 和 streamlit，真实训练前需要安装依赖。
- `compileall` 生成了 `__pycache__`，但已被 `.gitignore` 排除，不会进入 GitHub。
- 第三阶段提交后 GitHub HTTPS 连接连续超时，当前本地分支暂时领先远程 1 个提交。
- 第四阶段提交后 GitHub HTTPS 再次连接超时，当前本地提交暂未推送到远程。

## 下一步待办

- 提交并推送第二阶段代码骨架。
- 第三阶段实现 EuroSAT 数据模块和小批量 smoke test。
- 确认依赖安装方案和本机设备情况。
- 运行 `data_smoke --check-only` 验证配置和依赖状态。
- 安装 torch、torchvision 后运行真实 EuroSAT batch smoke test。
- 继续尝试将第三阶段提交同步到 GitHub。
- 运行单 batch 训练 smoke test，确认模型、数据、损失函数和优化器链路。
- 运行限制 batch 的短程 baseline，确认训练指标和 checkpoint 输出。
- 使用 `quick_baseline_best.pt` 运行独立评估脚本，确认 checkpoint 评估闭环。
- 基于中等 baseline 结果，继续扩大训练 batch 或进入增强训练策略对比。
- 使用实验汇总表作为后续报告实验结果和增强策略对比的基础。
- 运行同规模增强实验，并与 `medium_baseline_20b` 对比验证效果。
- 扩大训练步数或训练 epoch，观察 Mixup 和增强策略是否在更充分训练后超过 baseline。

## 验证记录

- 已检查待提交文件清单。
- 已确认 `outputs/`、`__pycache__/`、课程原件和 `.DS_Store` 不会被提交。
- 已运行 `python3 -m compileall src app`，语法检查通过。
- 已运行 `python3 -m src.eurosat_landuse.data_smoke --config configs/default.yaml --check-only`，配置读取和依赖状态检查通过。
- 已运行 `python3 -m src.eurosat_landuse.data_smoke --config configs/default.yaml`，在缺少 torch 和 torchvision 时能给出明确安装提示。
- 已创建第三阶段本地提交，推送暂因 GitHub 连接超时未完成。
- 已安装 torch 2.12.0、torchvision 0.27.0、timm 1.0.27、streamlit 1.58.0。
- 已确认 Apple MPS 可用。
- 已运行 `python3 -m src.eurosat_landuse.data_smoke --config configs/default.yaml --download --batch-size 8`，真实数据 smoke test 通过。
- EuroSAT 划分结果为 train 18900、val 4050、test 4050；训练 batch 形状为 `(8, 3, 224, 224)`。
- 已运行 `python3 -m compileall src app`，第四阶段新增代码语法检查通过。
- 已确认 `efficientformerv2_s0` 可由当前 timm 版本创建。
- 已运行 `python3 -m src.eurosat_landuse.train --config configs/default.yaml --smoke-test --download --batch-size 4`，单 batch 训练 smoke test 通过。
- 训练 smoke test 使用 MPS 设备，模型参数量为 3249796，logits 形状为 `(4, 10)`，loss 为 2.3244。
- 已创建第四阶段本地提交，推送暂因 GitHub 连接超时未完成。
- 已将第四阶段 smoke test 提交同步到 GitHub。
- 已运行 `python3 -m src.eurosat_landuse.train --config configs/default.yaml --download --epochs 1 --batch-size 8 --max-train-batches 5 --max-val-batches 2 --run-name quick_baseline`，短程 baseline 通过。
- 短程 baseline 结果：train_loss 2.3039，train_acc 0.0500，val_loss 2.3112，val_acc 0.0625。
- 已确认 `outputs/metrics/quick_baseline.json` 和 `outputs/checkpoints/quick_baseline_best.pt` 生成成功且被 `.gitignore` 排除。
- 已运行 `python3 -m src.eurosat_landuse.evaluate --config configs/default.yaml --checkpoint outputs/checkpoints/quick_baseline_best.pt --split val --batch-size 8 --max-batches 2 --run-name quick_eval_val`，独立评估脚本验证通过。
- 独立评估结果：val_loss 2.3112，val_acc 0.0625，评估样本数 16。
- 已确认 `outputs/metrics/quick_eval_val.json` 包含逐类准确率结构，并被 `.gitignore` 排除。
- 已运行 `medium_baseline_20b`：1 个 epoch，20 个训练 batch，10 个验证 batch，batch size 16。
- `medium_baseline_20b` 训练结果：train_loss 2.1405，train_acc 0.3000，val_loss 2.0115，val_acc 0.3563。
- 已使用独立评估脚本评估 `medium_baseline_20b_best.pt`，验证集 160 个样本，loss 2.0115，accuracy 0.3563。
- 已确认 `medium_baseline_20b` 的 metrics、checkpoint 和评估 JSON 均被 `.gitignore` 排除。
- 已运行 `python3 -m src.eurosat_landuse.summarize_experiments --config configs/default.yaml`，生成本地 `experiment_summary.csv` 和 `experiment_summary.md`。
- 实验汇总共识别 4 条记录，包含 quick baseline、中等 baseline 及对应验证集评估结果。
- 已运行 `python3 -m src.eurosat_landuse.train --config configs/enhanced.yaml --download --epochs 1 --batch-size 16 --max-train-batches 20 --max-val-batches 10 --run-name enhanced_20b`。
- `enhanced_20b` 结果：train_loss 2.1764，train_acc 0.1906，val_loss 2.1020，val_acc 0.2812。
- 已使用独立评估脚本评估 `enhanced_20b_best.pt`，验证集 160 个样本，loss 2.1020，accuracy 0.2812。
- 同规模短训下 enhanced 暂低于 `medium_baseline_20b` 的 val_acc 0.3563，后续需要更长训练验证增强策略效果。
- 已重新生成实验汇总表，当前包含 6 条训练/评估记录。
- 已创建增强训练配置本地提交，GitHub HTTPS 推送连续超时，当前本地分支暂时领先远程 1 个提交。
